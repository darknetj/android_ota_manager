[Unit]
Description=Copperhead Android OTA Server
After=network.target

[Service]
Type=simple
User=android_ota_server
ExecStart=/usr/local/bin/android_ota_server -env=production -config=/var/lib/android_ota_server/config.yml
Restart=on-abort

[Install]
WantedBy=multi-user.target
